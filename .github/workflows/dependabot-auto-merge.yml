name: dependabot-auto-merge

on: pull_request

permissions: 
  contents: write
  pull-requests: write

jobs:
  dependabot:
    if: ${{ github.actor == 'dependabot[bot]' }}
    uses: laurinstapf/laurinstapf/.github/workflows/dependabot-auto-merge.yml@main
